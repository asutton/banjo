
def f0 : () -> void { }
def f1 : (x : int, y : int) -> int { return x; }
def f2 : (x, y) -> int { return x; }
def f3 : (x, y) -> int = x;

def f4 : (x, y) { return x; }
def f5 : (x, y) = x + 1;
def f6 : (x, y) x * y;
