
concept Eq<typename T>
{
  requires (T a, T b) {
    a == b : bool;
    a != b : bool;
  }
}


concept Ord<typename T>
{
  requires (T a, T b) {
    a < b : bool;
    a > b : bool;
    a <= b : bool;
    a >= b : bool;
  }
}
